services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: patrimoine
      POSTGRES_PASSWORD: patrimoine
      POSTGRES_DB: patrimoine_jeu
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
